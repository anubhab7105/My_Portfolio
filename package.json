
{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && cp *.html *.css *.js public/ && cp -r images public/",
    "postbuild": "sed -i '' 's/?v=1.0/?v='$(date +%s)'/g' public/index.html"
  }
}